# URL du backend Portail utilisé par shared-components pour thèmes/langues
# En local Vite, garder une URL absolue (ex: http://localhost:8080) :
# le frontend la convertit automatiquement en proxy same-origin /portail-api.
VITE_PORTAIL_API_ORIGIN=
